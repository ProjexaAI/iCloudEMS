[project]
name = "icloudems-server"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.5",
    "curl_cffi>=0.5",
    "requests>=2.31",
]
